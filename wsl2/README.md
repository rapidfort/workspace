# install debian wsl2 distribution from microsoft store
# Run as Root: cp wsl.conf to /etc/wsl.conf on debian system
# on windows powershell terminate running debian distribution
wsl --terminate debian

